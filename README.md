# Pam's HarvestCraft

Custom Forge 1.20.1 mod for the InfrastructureSickos Minecraft server.

## Building

```bash
./gradlew build
```

## Publishing

Push a tag matching `v*` to trigger the CI workflow which builds and publishes to Modrinth.
